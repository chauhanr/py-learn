import logging
logging.basicConfig(level=logging.INFO,format='%(asctime)s-%(name)s-%(levelname)s=%(message)s', datefmt='%m/%d/%Y')

import helper # this will force the logger in helper to be initailed and the module will be `helper`    
